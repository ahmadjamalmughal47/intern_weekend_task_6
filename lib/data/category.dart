import 'package:flutter/material.dart';

class Category {
  Icon trailing;
  Icon leading;
  String title;
  String itemsRemaining;
  Category(this.trailing, this.leading, this.title, this.itemsRemaining);
}
